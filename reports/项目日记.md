# 项目日记

> 说明：本文件按时间顺序保留项目演化过程中的关键决策。早期条目对应单源数据阶段，文档末尾 `2026-04-09（多源数据集重跑后）` 一节给出了当前正式口径下的最新决策与状态。

## 2026-04-03

### 决策 1：以地理分组而非随机划分作为主评估协议

- 动机：数据天然带有省-市-县层级结构，随机划分会把同一地区的统计规律泄漏到测试集
- 证据：随机划分最优 RMSE `10.2219`，而按城市分组最优 RMSE `12.5892`
- 结论：主论文结论采用 `GroupKFold(CITY)`，随机划分只保留为补充结果

### 决策 2：移除 `COUNTY` 作为主类别特征

- 动机：`COUNTY` 在该数据中近似一条样本对应一个类别，容易形成“伪地理 ID”
- 目标：减少对局部类别记忆的依赖，提升跨城市泛化
- 结果：模型行为更稳定，线性与树模型不再被县级近唯一标识严重干扰

### 决策 3：保留强基线广覆盖，而不只押单个模型

- 动机：毕业设计需要完整性，必须覆盖线性模型、Bagging、Boosting、可选 foundation model
- 目标：形成可信的论文主结果表，并为创新模型选择合适骨干
- 结果：确认 `RF` 是主协议下最强单模型，`CatBoost` 在随机划分下最强

### 决策 4：TabPFN 只作为文献参考与接口保留，不强行纳入主实验

- 动机：TabPFN 官方流程要求首次授权与令牌配置；当前无头环境缺少 `TABPFN_TOKEN`
- 风险：若硬接入，将使无人值守流程中断
- 结果：代码层面保留接口，实验层面明确记录跳过原因，论文层面作为相关工作与扩展方向讨论

### 决策 5：创新模型从“全量三分支”收敛为“最优可执行子配置”

- 初始设想：全局专家 + 地理残差分支 + PFN/先验专家 + 动态融合
- 实际验证：
  - `CatBoost + HGBT stacker` 过拟合
  - `RF + 残差校正` 有轻微收益
  - `RF + LightGBM + 线性 stacking` 收益最稳定
  - 全模型比简化版没有进一步提升
- 最终选择：将 `GeoPFNMix-Lite` 作为论文中的推荐模型配置

### 决策 6：采用线性 stacking 替代 HGBT 元学习器

- 动机：小样本下 OOF 元特征容易被高容量元学习器过拟合
- 目标：让融合器只学习稳定、低自由度的组合关系
- 结果：融合实验从退化变为稳定提升

### 决策 7：解释性分两层完成

- 第一层：最终模型用 OOF 误差、预测散点、省级 MAE 做结果分析
- 第二层：对最终模型的全局 RF 专家做 SHAP，提取主要因子
- 原因：堆叠异构模型难以直接用单一 SHAP 完整解释，而“整体误差分析 + 主导专家 SHAP”更稳妥

### 决策 8：最终稿中强调“方法筛选过程”而不是“强行保留所有模块”

- 动机：毕业设计的创新不只是提出复杂结构，更重要的是通过消融说明哪些模块真的有效
- 表达策略：
  - 保留 GeoPFNMix 总体框架
  - 将 `GeoPFNMix-Lite` 作为最终部署配置
  - 将残差分支作为受控消融与未来优化方向

## 当前项目状态

- 数据审计：完成
- 基线实验：完成
- 创新模型：完成并确定最终配置
- 消融与显著性检验：完成
- 图表资产：完成
- 实验日志：完成
- 论文初稿：待随本次提交一并落地

## 后续可扩展方向

- 若获得 `TABPFN_TOKEN`，补跑 TabPFN 回归实验并纳入对比
- 尝试更强的区域先验，如省份图结构或检索式 foundation model
- 若后续有经纬度、土地利用、交通强度等字段，可进一步做空间增强建模

## 2026-04-08

### 决策 9：以当前本地重跑结果作为论文修订与图表更新的统一口径

- 动机：仓库内早期结果来自另一套环境，本地无人值守复现后出现了可观但可解释的结果漂移
- 做法：完整重跑 `EDA + baseline + GeoPFNMix + analysis assets`
- 结果：论文、README、图表、日志统一以当前 `artifacts/` 中最新文件为准

### 决策 10：将 `geopfnmix_catboost` 纳入正式消融，而不是只保留早期 RF 系列配置

- 动机：本地快速补测表明，`CatBoost` 作为全局专家时，完整 GeoPFNMix 能稳定优于最强单模型 `CatBoost`
- 结果：
  - `GeoPFNMix-CatBoost` 成为当前最优最终配置
  - `GeoPFNMix-Lite` 继续保留为简洁对照

### 决策 11：图表中的区域标签统一改为英文，降低字体依赖

- 动机：本地图形环境对中文省名标签显示不稳定，影响论文截图质量
- 实施：
  - `province_level_mae.png` 坐标轴省名改为英文
  - 新增分析图表继续统一使用英文轴标签与标题
- 结果：图表在本地与无人值守环境中显示更稳定

### 决策 12：让分析脚本围绕“当前最佳变体”自动出图，而不是硬编码旧模型

- 动机：如果最佳配置变化，手工改图名和数据源容易遗漏
- 实施：
  - `run_analysis_assets.py` 自动读取消融结果并选择最佳 GeoPFNMix 变体
  - 解释性分析自动匹配最佳变体的全局专家
- 结果：论文资产生成流程更稳健，也更适合后续继续迭代

### 决策 13：README 从“实验入口说明”升级为“复现与接口文档”

- 动机：当前项目已不仅是代码集合，还需要承担答辩展示、复现实验和后续维护说明的作用
- 补充内容：
  - 模型接口
  - 运行方法
  - GeoPFNMix 家族说明
  - 输出文件说明
  - 无人值守与 TabPFN 回退策略

## 当前项目状态（2026-04-08 更新）

- 数据审计：完成并本地复核
- 基线实验：完成并本地重跑
- 创新模型：完成并补充 `GeoPFNMix-CatBoost` 变体
- 图表资产：扩充完成
- 论文初稿：进入第二轮结构化修订
- README：已补充为较完整的复现与接口说明

## 2026-04-09

### 决策 14：正式把 `token.py` 作为本地私有认证入口

- 动机：用户明确要求在无人值守环境中通过 `import token.py` 读取 `HF_TOKEN`
- 实施：
  - 在项目代码中增加本地令牌解析模块
  - 将 `token.py` 加入 `.gitignore`
- 结果：
  - `TabPFN` 已可在当前环境中头less 运行
  - 不需要在脚本执行过程中手工输入令牌

### 决策 15：不再把 TabPFN 视为“未来工作”，而是纳入正式主结果

- 动机：认证探针与最小化训练均已成功
- 实施：
  - 重跑 `run_baselines.py`
  - 将 `TabPFN` 写入正式结果表
- 结果：
  - `TabPFN` 成为当前三套协议下最强单模型
  - 论文主线从“树模型最强”更新为“TabPFN 单模型最强，但融合仍可继续增益”

### 决策 16：将 `geopfnmix_tabpfn` 也纳入正式消融

- 动机：仅测试 Lite-TabPFN 和 CatBoost-TabPFN 还不足以回答“完整 GeoPFNMix 在更强先验下是否仍值得保留”
- 实施：
  - 扩充 `run_geopfnmix.py` 的实验列表
  - 补充 `TabPFN` 对照和更多 Wilcoxon 显著性检验
- 结果：
  - `GeoPFNMix-TabPFN` 明显优于 `GeoPFNMix`
  - 但仍显著弱于 `GeoPFNMix-Lite-TabPFN`
  - 这使“Lite 更适合承接强先验”的结论更扎实

### 决策 17：论文叙事从“单点最优模型”升级为“方法家族演化”

- 动机：用户特别要求重点介绍 `GeoPFNMix`、`GeoPFNMix-Lite`、`GeoPFNMix-CatBoost` 的提出、动机与设计目标
- 表达策略：
  - GeoPFNMix 作为总框架
  - GeoPFNMix-Lite 作为复杂度控制与稳定交付版本
  - GeoPFNMix-CatBoost 作为增强全局专家的路线
  - TabPFN 版本作为先验增强路线
- 结果：
  - 论文结构更像一条完整研究路径
  - 答辩时更容易解释“为什么不是只报一个最优分数”

## 当前项目状态（2026-04-09 更新）

- 数据审计：完成
- 基线实验：完成并纳入 `TabPFN`
- 创新模型：完成并补充 `GeoPFNMix-TabPFN`
- 图表资产：完成并与最佳变体自动对齐
- 实验日志：更新到 TabPFN 正式纳入阶段
- 论文初稿：进入第三轮充实与润色
- README：进入与最新结果同步的修订阶段

## 2026-04-09（多源数据集重跑后）

### 决策 18：将 `data/` 多源数据目录升级为正式数据入口

- 动机：原始中国单源数据规模和分布覆盖有限，不足以支撑更稳健的跨区域结论
- 实施：
  - 将 Australia、Brazil、China、EU、USA 五个来源统一纳入
  - 标准化为 `COUNTRY`、`PROVINCE`、`CITY`、`COUNTY` 与统一数值字段
- 结果：
  - 正式样本数扩展到 `12445`
  - `COUNTRY` 成为必须建模的正式类别特征
  - `GroupKFold(CITY)` 仍保留为主协议，但其含义升级为“统一二级地理组”

### 决策 19：允许为不同来源构造代理层级，而不强求原始层级完全一致

- 动机：多源数据中的地理编码粒度并不统一，若强行只保留原始“城市”概念，会失去一部分数据
- 实施：
  - Australia/Brazil/EU 通过原始编码恢复一级与二级代理组
  - USA 以 `State` 作为一级组与二级组代理，`County` 只保留为元信息
- 结果：
  - 统一了跨来源实验接口
  - 避免了单样本组大量爆炸的问题

### 决策 20：以 Colab A100 GPU 重跑结果作为当前正式结论来源

- 动机：`run_geopfnmix.py` 在本地 CPU 环境下耗时较长，而 `TabPFN`、`CatBoost` 和部分 Boosting 模型可从 GPU 中明显受益
- 实施：
  - 为脚本增加 `--device auto/cpu/gpu`
  - 在 Colab A100 上执行 `run_baselines.py --device gpu` 与 `run_geopfnmix.py --device gpu`
  - 将最新 `artifacts/` pull 回本地仓库
- 结果：
  - 正式实验速度和稳定性显著提升
  - 论文与日志统一以 GPU 重跑后的 `artifacts/` 为准

### 决策 21：重新定位 GeoPFNMix 家族的主叙事

- 动机：多源数据重跑后，最优单模型和最优融合模型都发生了变化
- 新结论：
  - `TabPFN` 只在 `RandomKFold` 下保持最强单模型
  - `CatBoost` 成为 `GroupKFold(CITY)` 与 `GroupKFold(PROVINCE)` 下的最强单模型
  - `GeoPFNMix-CatBoost-TabPFN` 成为当前最佳融合模型
- 叙事调整：
  - GeoPFNMix：总框架
  - GeoPFNMix-Lite：复杂度控制与 RF 骨干强对照
  - GeoPFNMix-CatBoost：当前最有效的主线演化方向

### 决策 22：正式采用 `HF_token.py` 作为私有认证文件名

- 动机：避免与 Python 标准库 `token` 命名冲突
- 实施：
  - 代码优先读取 `HF_token.py`
  - 保留对早期 `token.py` 的兼容
  - `.gitignore` 中忽略 `HF_token.py`
- 结果：
  - 无人值守认证链路更稳妥
  - Colab、本地和后续服务器环境的说明口径更统一

## 当前项目状态（多源数据集重跑后）

- 数据审计：完成并切换为多源数据版本
- 基线实验：完成并正式确认 `CatBoost` 为严格分组协议下最强单模型
- 创新模型：完成并正式确认 `GeoPFNMix-CatBoost-TabPFN` 为当前最优融合模型
- 图表资产：完成并同步到最新 `artifacts/`
- 实验日志：已更新到多源数据与 Colab GPU 重跑阶段
- 论文终稿：进入提交版精修与结果统一阶段
- README：已同步到 `HF_token.py`、多源数据和 `--device` 参数口径

## 2026-04-10（补充实验脚本准备）

### 决策 23：新增 `GeoPFNMix-Lite-CatBoost` 作为正式补充变体

- 动机：
  - 当前已经有 `GeoPFNMix-Lite` 与 `GeoPFNMix-CatBoost`
  - 但缺少“Lite 结构 + CatBoost 骨干”这一中间对照
- 实施：
  - 在 `make_model` 中新增 `geopfnmix_lite_catboost`
  - 在 `run_geopfnmix.py` 中将其纳入正式消融和显著性比较
- 目的：
  - 回答 Lite 的收益是否会因更强全局专家而显著改变
  - 为论文讨论“复杂度控制”和“增强骨干”之间的关系提供更细证据

### 决策 24：把“最佳模型按数据源拆分表现”升级为正式分析资产

- 动机：
  - 当前只有全球整体 OOF 表现，不足以回答最佳模型在不同来源数据上的推理差异
- 实施：
  - 在 `run_analysis_assets.py` 中新增按 `COUNTRY` 统计的指标表与图
  - 输出：
    - `best_model_country_metrics.csv`
    - `best_model_country_metrics.png`
- 目的：
  - 让论文可以直接展示 Australia、Brazil、China、EU、USA 各自的表现
  - 判断当前最佳模型是否存在明显的来源偏置或来源短板

## 当前项目状态（2026-04-10）

- 脚本准备：已完成
- 新补充变体：`GeoPFNMix-Lite-CatBoost` 已接入
- 新分析资产：最佳模型按数据源拆分指标已接入
- 正式结果：待 Colab GPU 重跑后回填
- 论文与 README：已同步到“补充实验待执行”口径

## 2026-04-10（补充实验结果回填后）

### 决策 25：将 `GeoPFNMix-Lite-CatBoost` 纳入正式方法讨论

- 结果：
  - `GeoPFNMix-Lite-CatBoost` 在主协议下达到 `RMSE=5.9564`
  - 显著优于单模型 `CatBoost`（`p = 1.70e-26`）
  - 与 `GeoPFNMix-CatBoost` 极为接近，但后者仍显著更优（`p = 2.48e-21`）
- 解释：
  - 这说明在 CatBoost 骨干下，Lite 结构已经能够吃到大部分融合收益
  - 但残差分支仍保留了稳定且可检出的边际贡献

### 决策 26：将“按数据源拆分表现”提升为论文正式证据

- 结果：
  - 当前最佳模型在 China 上误差显著更高
  - Brazil、Australia、EU 的绝对误差明显较低
  - USA 出现“绝对误差低但 `R²` 为负”的特殊现象
- 解释：
  - 全球整体指标会掩盖来源间的真实难度差异
  - 后续论文讨论必须把“多源异质性”从抽象判断落到具体来源表现上

## 当前项目状态（补充实验结果回填后）

- 当前最佳均值模型：`GeoPFNMix-CatBoost-TabPFN`
- 新中间强对照：`GeoPFNMix-Lite-CatBoost`
- 新正式分析资产：最佳模型按数据源拆分表现
- 论文状态：进入基于新补充实验的结果回填与讨论加强阶段

## 2026-04-10（第二轮补充实验脚本准备）

### 决策 27：新增 `GeoPFNMix-Lite-CatBoost-TabPFN` 作为 Lite 路线的强先验版本

- 动机：
  - `GeoPFNMix-Lite-CatBoost` 已经非常接近当前最优结果
  - 这提示“更强骨干 + Lite 结构”可能已经足以吸收主要收益
  - 仍需要进一步验证在该结构上接入 `TabPFN` 后，能否以更低复杂度逼近或刷新当前最优
- 实施：
  - 在 `make_model` 中新增 `geopfnmix_lite_catboost_tabpfn`
  - 在 `run_geopfnmix.py` 中纳入正式消融与显著性比较
- 目的：
  - 检验“残差分支是否仍是必要条件”
  - 检验“Lite 结构 + 强先验”是否能成为新的主力候选

### 决策 28：将来源拆分分析升级为多模型并排对照

- 动机：
  - 只展示最佳模型来源拆分表现，无法解释它为何比对照模型更优
- 实施：
  - 在 `run_analysis_assets.py` 中新增“当前最优两个 GeoPFNMix 家族模型 + CatBoost + TabPFN”的按来源对照资产
  - 新增输出：
    - `selected_models_country_metrics.csv`
    - `selected_models_country_metrics.png`
- 目的：
  - 让来源层面的比较从“单模型现象描述”提升为“多模型差异解释”
  - 为论文结果与讨论部分补足更强的横向证据

## 当前项目状态（第二轮补充实验脚本准备后）

- 新补充变体：`GeoPFNMix-Lite-CatBoost-TabPFN` 已接入
- 新分析资产：多模型按数据源拆分对照已接入
- 正式结果：待 Colab GPU 重跑后回填
- 文档状态：README、实验日志、项目日记与项目分析已同步到“第二轮补充实验待执行”口径

## 2026-04-14（第二轮补充实验结果回填后）

### 决策 29：将 `GeoPFNMix-Lite-CatBoost-TabPFN` 纳入正式方法主线

- 结果：
  - `GeoPFNMix-Lite-CatBoost-TabPFN` 在主协议下达到 `RMSE=5.9281`
  - 成为当前第二优融合配置
  - 相对 `GeoPFNMix-Lite-CatBoost` 的提升达到统计显著（`p = 1.17e-02`）
- 解释：
  - 这说明在 CatBoost 骨干的 Lite 路线上，TabPFN 先验仍然能够提供可检出的稳定增益
  - 但它仍显著弱于 `GeoPFNMix-CatBoost-TabPFN`，说明残差分支在强骨干 + 强先验组合下依旧有独立价值

### 决策 30：把来源拆分分析从“描述最佳模型”升级为“比较模型在哪些来源上获胜”

- 结果：
  - `GeoPFNMix-CatBoost-TabPFN` 在 `Australia`、`Brazil` 和 `China` 上表现最优
  - 单模型 `CatBoost` 在 `EU` 与 `USA` 上更稳健
  - `TabPFN` 单模型在 `USA` 上出现明显失稳
- 解释：
  - 这说明全球冠军模型的优势并不是对所有来源一视同仁
  - GeoPFNMix 家族的总体领先，主要来自对困难来源和大样本来源的更强修正能力

## 当前项目状态（第二轮补充实验结果回填后）

- 当前最佳均值模型：`GeoPFNMix-CatBoost-TabPFN`
- 当前第二优融合模型：`GeoPFNMix-Lite-CatBoost-TabPFN`
- 新正式分析资产：多模型按数据源拆分对照
- 论文状态：进入基于第二轮补充实验的新一轮结果回填与表达优化阶段
