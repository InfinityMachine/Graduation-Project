# 毕业设计总体计划（PM2.5 预测，基于农业数据集 data.csv）

## 0. 课题定义与目标

**课题**：基于县域尺度农业与环境特征，预测空气中 PM2.5 含量。  
**任务类型**：监督学习中的表格回归（Tabular Regression）。  
**核心目标**：
1. 构建一个性能稳定、泛化能力强的 PM2.5 预测模型；
2. 在方法上体现毕业设计应有的完整性（数据工程+模型设计+对比实验+可解释性+论文）；
3. 在已有模型（如 Limix 思想与 TabPFN 思想）基础上提出可落地的创新点。

---

## 1. 数据理解与问题拆解

### 1.1 数据初步画像（已完成快速勘察）
- 样本规模：2676 条（县域样本）。
- 字段数：13 列。
- 目标列：`PM2.5`。
- 特征类型：
  - 类别特征：`PROVINCE`, `CITY`, `COUNTY`
  - 数值特征：`AET`, `ppt`, `tem`, `wind`, `NOX`, `SO2`, `fertilzier`, `manure`（以及可选 ID）
- 缺失：当前数据中未发现显式缺失（空字符串 / NaN）。

### 1.2 关键挑战
1. **样本量偏中小（2676）**：深度模型容易过拟合。
2. **地理层级结构明显（省/市/县）**：常规随机划分易产生“地理泄漏”。
3. **特征异质性强**：农业投入、气象变量、污染前体物共同影响 PM2.5，存在非线性与交互。
4. **目标存在空间异质性**：同类农业活动在不同区域气候背景下影响不同。

---

## 2. 技术路线总览（从“稳健基线”到“创新模型”）

采用“**三层模型体系**”：

### 层 A：强基线（保证下限）
- 线性回归 / Ridge / Lasso
- 随机森林回归
- XGBoost / LightGBM / CatBoost（表格任务常见强基线）

### 层 B：前沿表格方法（对标）
- **TabPFN 思路**：利用预训练表格先验（小样本场景非常有价值）
- **FT-Transformer / TabTransformer**：作为深度表格模型对照

### 层 C：毕业设计主模型（创新）
提出：

> **Geo-PFN-Mix：地理分层先验 + 混合效应残差校正 + 多专家集成**

由三部分组成：
1. **全局非线性主干**：GBDT 或 FT-Transformer 学习全局非线性关系；
2. **分层随机效应分支（Limix思想）**：对省/市层级建随机效应项，校正区域偏置；
3. **PFN/元学习小样本增强分支**：对低样本区域进行先验增强预测；
4. 最终由一个轻量 **stacking 元学习器** 融合，输出 PM2.5 预测。

---

## 3. 可执行的模型设计细节

## 3.1 数据预处理与特征工程

### (1) 质量控制
- 去除不合理 ID 特征直接泄漏风险（`OBJECTID_1` 默认不进模型）
- Winsorize/分位裁剪处理极端值（如 NOX、SO2、fertilzier、manure）
- 对偏态特征进行 `log1p` 变换（与不变换版本做消融）

### (2) 编码策略
- 类别变量采用：
  - CatBoost 原生编码（用于 CatBoost）
  - Target Encoding（用于线性/GBDT对照）
  - Embedding（用于 Transformer 类模型）

### (3) 交互特征（可解释且有机理）
- 农业投入 × 气象：`fertilzier*wind`, `manure*ppt`, `NOX*tem` 等
- 污染协同：`NOX/SO2`, `NOX+SO2`
- 区域强度标准化：特征在省内 z-score（捕获“相对强度”）

### (4) 分组划分策略（重点）
- 训练/验证/测试优先使用 **GroupKFold（按 CITY 或 PROVINCE）**
- 同时报告随机 KFold 结果，但以分组结果为主结论，避免地理泄漏导致虚高性能。

---

## 3.2 主模型：Geo-PFN-Mix（创新点）

### 模块 A：Global Nonlinear Expert（全局专家）
- 使用 LightGBM/CatBoost 训练主预测器：
  \[
  \hat{y}_{global} = f_{gbdt}(x)
  \]
- 优势：对中小规模表格与非线性交互鲁棒，训练稳定。

### 模块 B：Hierarchical Mixed-Effect Residual（层级混合效应残差）
- 学习残差：
  \[
  r = y - \hat{y}_{global}
  \]
- 在残差上拟合分层随机效应：
  \[
  r = b_{province} + b_{city(province)} + \epsilon
  \]
- 该分支借鉴 Limix / LMM 思路，捕捉“地理层级系统偏差”。

### 模块 C：Regional Few-shot PFN Expert（区域小样本专家）
- 对样本少、分布偏移大的城市/省份，构建 PFN/元学习专家：
  - 输入：该区域局部数据 + 邻近区域支持集
  - 输出：\(\hat{y}_{pfn}\)
- 实现上可先采用 TabPFN 推理 + 简单校准头（线性/MLP）。

### 模块 D：动态融合（stacking）
- 最终输出：
  \[
  \hat{y} = w_1 \hat{y}_{global} + w_2 \hat{r}_{mixed} + w_3 \hat{y}_{pfn}
  \]
- 权重由元学习器根据区域与不确定性指标动态给出。

---

## 4. 实验设计（确保“论文级”完整性）

## 4.1 评价指标
- 主指标：RMSE、MAE、R²
- 稳健性：各折均值±标准差
- 分组评估：按省/城市分桶误差（公平性与泛化）

## 4.2 对比实验（必须）
1. 线性模型组（LR/Ridge/Lasso）
2. 树模型组（RF/XGBoost/LightGBM/CatBoost）
3. 深度模型组（FT-Transformer）
4. 先验模型组（TabPFN）
5. **本文模型（Geo-PFN-Mix）**

## 4.3 消融实验（创新可信度关键）
- 去掉混合效应分支（验证地理层级建模价值）
- 去掉 PFN 分支（验证先验增强价值）
- 固定融合权重 vs 动态权重
- 去掉交互特征 / log 变换

## 4.4 统计显著性
- 使用配对 t 检验或 Wilcoxon 检验，比较主模型与最佳基线误差差异。

## 4.5 可解释性
- SHAP 全局重要性与局部解释
- PDP/ICE 曲线分析关键特征（NOX、SO2、fertilzier、wind 等）
- 分区域解释：不同省份中主导因素差异

---

## 5. 论文撰写与交付结构

## 5.1 论文目录建议
1. 绪论（背景、意义、研究问题）
2. 相关工作（PM2.5 预测、表格学习、混合效应模型、PFN）
3. 数据集与问题定义（数据描述、预处理、评估协议）
4. 方法（Geo-PFN-Mix 各模块与训练流程）
5. 实验（对比、消融、可解释性、误差分析）
6. 结论与展望

## 5.2 图表清单（至少）
- 数据分布图（目标与关键特征）
- 模型架构图（主图）
- 主结果表（各模型指标）
- 消融结果表
- SHAP 总结图 + 典型样本解释图
- 分省误差热力图

## 5.3 参考文献策略
- PFN / TabPFN 原始论文
- Limix / 线性混合效应基础文献
- 表格深度学习代表论文（FT-Transformer 等）
- PM2.5 预测相关应用文献（近5年为主）

---

## 6. 里程碑时间表（12 周）

- **第1–2周**：数据审计、EDA、划分协议确定、基线复现
- **第3–4周**：树模型+调参，建立强 baseline
- **第5–6周**：TabPFN / FT-Transformer 对照实验
- **第7–8周**：实现混合效应残差分支与融合框架
- **第9周**：完成消融、显著性检验、误差分析
- **第10周**：可解释性与可视化图表定稿
- **第11周**：论文初稿（方法+实验）
- **第12周**：论文终稿、答辩 PPT、代码与复现实验包

---

## 7. 风险与预案

1. **TabPFN 在回归任务兼容性/资源问题**  
   预案：先以 CatBoost + 混合效应作为主线，PFN 作为增强分支，不阻塞整体进度。

2. **分组划分导致性能下降**  
   预案：通过交互特征、分层随机效应与模型集成提升跨区域泛化。

3. **创新点不够突出**  
   预案：确保“地理分层残差 + 小样本先验增强 + 动态融合”三者共同构成方法创新，并用消融量化每一部分贡献。

---

## 8. 最终交付件

- 可复现实验代码（含随机种子、配置、日志）
- 训练完成模型与推理脚本
- 完整实验报告与论文（含图、表、参考文献）
- 答辩材料（PPT + 核心结果图）

---

## 9. 下一步立即执行（本周）

1. 固化数据划分脚本（RandomKFold + GroupKFold）  
2. 跑出 5 个强基线（LR、RF、XGBoost、LightGBM、CatBoost）  
3. 建立统一评估模板（RMSE/MAE/R² + 分组误差）  
4. 输出第一版 EDA 图和 baseline 结果表（可直接进入论文第4章）
